# @moxiu/utils

Utility helpers used across packages.

Install

```
npm install @moxiu/utils
```

Build / Develop

- Build: `npm run build`
- Dev (watch): `npm run dev`
- Test: `npm run test`
- Lint: `npm run lint`

Usage

```ts
import { someUtil } from '@moxiu/utils'
```
