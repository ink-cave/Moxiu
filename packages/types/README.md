# @moxiu/types

Shared TypeScript types used across packages.

Build / Develop

- Build: `npm run build`
- Dev (watch): `npm run dev`
- Test: `npm run test`
- Lint: `npm run lint`

Usage

```ts
import { MyType } from '@moxiu/types'
```
