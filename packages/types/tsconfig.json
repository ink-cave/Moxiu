{
  "extends": "../../tsconfig.json", // 继承根目录的基础配置
  "compilerOptions": {
    "outDir": "./dist", // 编译产物输出到 dist
    "rootDir": "./src", // 源码目录
    "noEmit": false // 允许编译（根目录是 noEmit: true）
  },
  "include": ["src/**/*"], // 包含 src 下所有文件
  "exclude": ["node_modules", "dist"]
}
