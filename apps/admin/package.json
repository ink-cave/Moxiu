{
  "name": "@moxiu/admin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsc --watch & NODE_ENV=development node --loader ts-node/esm src/index.ts",
    "build": "tsc -p .",
    "start": "node ./dist/index.js",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fastify": "^4.25.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1",
    "eslint": "^10.0.0"
  }
}
